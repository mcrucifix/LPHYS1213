Prenons par exemple le \cindex{premier principe de la thermodynamique}. Il nous dit que la variation d’énergie interne d’un parcelle de masse $M$ (de volume $\Vol$ variable) peut se produire soit par le \cindex{travail} exercé par cette parcelle sur son environnement, soit par un \cindex{échange de chaleur}. Si la transformation est \cindex{réversible}, on écrira simplement :
\begin{equation}
\Delta U = - p \, \Delta \Vol + T \, \Delta S, 
\label{eq:firstaw_discrete}
\end{equation}

où $U$ et $S$ sont l'énergie interne exprimées comme des variables extensives : c'est le total pour toute la parcelle. 
Ici, le prédicat \(\Delta\), majuscule indique une variation au cours du temps, et non un incrément dans l’espace (que nous avions noté \(\delta\)).  

Dans notre monde du \cindex{milieu continu}, où l’on conçoit la parcelle comme étant suffisamment petite dans un milieu suffisamment homogène, on considérera que les transformations sont réversibles. 

Vu la façon dont nous avons écrit le premier principe, nous devons faire référence à une parcelle de masse constante, mais de volume variable. C'est tout à fait envisageable. 
Toujours un peu à la hussarde, une parcelle de volume $\Vol$, de masse $M$ peut changer de volume au cours du temps : la variation $\Delta V = M \Delta (\frac{1}{\rho})$. 

Réécrivons notre équation \eqref{eq:firstaw_discrete}, mais en utilisant des variables intensives, exprimées par unité de masse plutôt que de volume, $U=M \umass$; $S=\rho\smass$, puis nous simplifions en divisant par $M$ tous les membres:

\begin{equation}
\Delta \umass = - p \, \Delta (\frac{1}{\rho}) + T \, \Delta s, 
\label{eq:firstaw_discrete2}
\end{equation}

C'est ici que la magie opère. Cette équation de conservation, que nous avons obtenue en raisonnant sur une parcelle en mouvement, et  que nous voulons \emph{aussi petite que l'on veut}, aboutit à une forme différentielle impliquant la dérivée \emph{matérielle}: 


\begin{equation}
\Dif_t \umass = - p \, \Dif_t \left(\frac{1}{\rho}\right) + T \, \Dif_t \smass
\label{eq:firstaw_continuous}
\end{equation}
avec
\begin{equation}
  \Dif_t \umass = \frac{\partial \umass}{\partial t} + \vec{u} \cdot \grad \umass\textrm{, etc.}
\label{eq:material_derivative_U}
\end{equation}
Félicitations ! Nous venons de décrire le premier principe de la thermodynamique en mécanique des fluides. Comme nous l’avons dérivé de façon un peu cavalière, nous devons nous rassurer en vérifiant la cohérence de l’écriture. 


-----

\section{Écoulement dans un plan}
\label{sec:ecoulement_plan}

\subsection{Objectif : Développer une intuition physique}
Pour comprendre comment la viscosité influence un écoulement et développer une intuition physique du sens du terme $\mu\lapl\u$, commençons par étudier un cas simple : un écoulement stationnaire et plan (dans le plan $(x,y)$), où la vitesse $\u$ ne dépend que de $y$ ($u_x = u_x(y)$). Nous négligeons ici les effets de la gravité ($\grad \gpot = 0$).

\subsubsection{Équation du mouvement pour un élément de volume}
Considérons un petit élément de volume de fluide, de dimensions $\dif x$, $\dif y$, et d'épaisseur unitaire (figure~\ref{fig:element_volume_plan}).

\begin{figure}[h]
\centering
%\includegraphics[width=0.6\textwidth]{Figures/element_volume_plan.pdf}
TODO: FIGURE
\caption{Schéma d'un élément de volume dans un écoulement plan. Les forces de cisaillement $\tau_{yx}$ agissent sur les faces supérieure et inférieure.}
\label{fig:element_volume_plan}
\end{figure}

Les forces agissant sur cet élément sont :
\begin{itemize}
    \item Les forces de pression : $-(\pd{p}{x}\dif x)\dif y$ dans la direction $x$.
    \item Les forces visqueuses : La contrainte de cisaillement $\tau_{yx} = \mu \pd{u_x}{y}$ agit sur les faces supérieure et inférieure. La différence de contrainte entre ces deux faces est $\left(\tau_{yx}(y+\dif y) - \tau_{yx}(y)\right)\dif x = \mu \pd[2]{u_x}{y} \dif x \dif y$.
\end{itemize}

TODO: modifier. utiliser les termes stationnaires et reprendre l'équation d'Euler ( En appliquant la deuxième loi de Newton à cet élément, on obtient :)
\begin{displaymath}
0 = -\pd{p}{x} + \mu \pd[2]{u_x}{y}.
\end{displaymath}

De même, dans la direction $y$, l'équilibre des forces donne:
\begin{displaymath}
0 = -\pd{p}{y}.
\end{displaymath}

Ainsi, la pression ne dépend pas de $y$ : $p = p(x)$.

\subsubsection{Solution générale}
En intégrant deux fois l'équation pour $u_x(y)$, on obtient :
\begin{displaymath}
\mu u_x(y) = \frac{y^2}{2} \pd{p}{x} + A y + B,
\end{displaymath}
où $A$ et $B$ sont des constantes d'intégration.

\subsection{Cas particuliers}
\subsubsection{Écoulement de Couette ($\pd{p}{x} = 0$)}
Si le gradient de pression est nul ($\pd{p}{x} = 0$), l'équation se réduit à :
\begin{displaymath}
\mu u_x(y) = A y + B.
\end{displaymath}

Supposons que le fluide est confiné entre deux plaques parallèles, l'une fixe ($u_x(0) = 0$) et l'autre en mouvement ($u_x(h) = U$). Les conditions aux limites donnent :
\begin{displaymath}
B = 0, \quad A = \frac{\mu U}{h}.
\end{displaymath}

La solution est donc un profil linéaire :
\begin{displaymath}
u_x(y) = \frac{U}{h} y.
\end{displaymath}


